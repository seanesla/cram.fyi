{
  "name": "cram.fyi",
  "private": false,
  "scripts": {
    "start": "node app/server.mjs",
    "check": "find app -name '*.mjs' -print0 | xargs -0 -n1 node --check && node --test app/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
